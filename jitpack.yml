before_install:
   - wget --no-check-certificate https://yarnpkg.com/latest.tar.gz
   - ls
   - tar zvxf latest.tar.gz
   - mv yarn-v* yarn # will never change
   - find .
install:
   - PATH="$PWD/yarn/bin/:$PATH" yarn -v
   - PATH="$PWD/yarn/bin/:$PATH" sbt -J-XX:MaxMetaspaceSize=1024M +publishM2
