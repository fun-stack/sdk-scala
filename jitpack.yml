before_install:
   - wget https://yarnpkg.com/latest.tar.gz
   - ls
   - tar zvxf latest.tar.gz
   - mv yarn-v* yarn # will never change
   - find .
install:
   - export PATH="$PWD/yarn/bin/:$PATH"
   - yarn -v
   - sbt -J-XX:MaxMetaspaceSize=1024M +publishM2
